<!DOCTYPE html>
<html>
  <head>
  <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="turbo-visit-control" content="reload">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <!-- Script do Bootstrap -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>    <title><%= content_for(:title) || "Juros" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Juros</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

  <style>
    /* Estilização do sidebar para desktop */
    @media (min-width: 768px) {
      #sidebarMenu {
        position: fixed;
        top: 0;
        left: 0;
        width: 240px;
        height: 100vh;
        background: #1a1a1a; /* Escuro para mais contraste */
        padding-top: 20px;
        border-right: 1px solid #333; /* Ligeiramente mais claro para contraste */
      }

      #mainContent {
      }

      /* Links do menu lateral */
      .nav-link {
        color: #ddd;
        font-size: 1rem;
        padding: 12px 15px;
        transition: all 0.3s;
        display: flex;
        align-items: center;
      }

      .nav-link i {
        margin-right: 10px;
      }

      /* Hover com mais destaque */
      .nav-link:hover {
        background: #333; /* Fundo escuro mais forte no hover */
        color: #fff;
        border-radius: 5px;
      }
    }

    /* Estilo do menu (mobile e desktop) */
    .offcanvas {
      background: #1a1a1a; /* Fundo escuro */
      color: #f4f4f4; /* Texto claro */
    }

    .offcanvas-title {
      font-weight: 600;
      font-size: 1.2rem;
      color: #fff; /* Cor mais forte para o título */
    }

    /* Links do menu (mobile) */
    .nav-link {
      color: #ddd;
      font-size: 1rem;
      padding: 12px 15px;
      transition: all 0.3s;
      display: flex;
      align-items: center;
    }

    .nav-link i {
      margin-right: 10px;
    }

    /* Hover com mais destaque */
    .nav-link:hover {
      background: #333; /* Fundo escuro mais forte no hover */
      color: #fff;
      border-radius: 5px;
    }

    /* Botão menu mobile */
    .menu-btn {
      background: #333; /* Cor mais escura para o botão */
      color: white;
      border: none;
      padding: 10px 15px;
      font-size: 1.2rem;
      border-radius: 5px;
      transition: 0.3s;
    }

    .menu-btn:hover {
      background: #444; /* Um pouco mais claro no hover */
    }

    /* Botão de fechar corrigido */
    .btn-close-white {
      filter: invert(1); /* Garantir que o ícone de fechar fique branco */
    }

    /* Cor do conteúdo principal */
    #mainContent {
      color: #333; /* Cor escura para o texto */
      background: #f4f4f4; /* Fundo claro para o conteúdo */
      padding: 20px;
      border-radius: 5px;
    }

     /* Estilização do sidebar para desktop */
     @media (min-width: 768px) {
      #sidebarMenu {
        position: fixed;
        top: 0;
        left: 0;
        width: 240px;
        height: 100vh;
        background: #1a1a1a; /* Escuro para mais contraste */
        padding-top: 20px;
        border-right: 1px solid #333; /* Ligeiramente mais claro para contraste */
      }

   

      /* Links do menu lateral */
      .nav-link {
        color: #ddd;
        font-size: 1rem;
        padding: 12px 15px;
        transition: all 0.3s;
        display: flex;
        align-items: center;
      }

      .nav-link i {
        margin-right: 10px;
      }

      /* Hover com mais destaque */
      .nav-link:hover {
        background: #333; /* Fundo escuro mais forte no hover */
        color: #fff;
        border-radius: 5px;
      }
    }

    /* Estilo do menu (mobile e desktop) */
    .offcanvas {
      background: #1a1a1a; /* Fundo escuro */
      color: #f4f4f4; /* Texto claro */
    }

    .offcanvas-title {
      font-weight: 600;
      font-size: 1.2rem;
      color: #fff; /* Cor mais forte para o título */
    }

    /* Links do menu (mobile) */
    .nav-link {
      color: #ddd;
      font-size: 1rem;
      padding: 12px 15px;
      transition: all 0.3s;
      display: flex;
      align-items: center;
    }

    .nav-link i {
      margin-right: 10px;
    }

    /* Hover com mais destaque */
    .nav-link:hover {
      background: #333; /* Fundo escuro mais forte no hover */
      color: #fff;
      border-radius: 5px;
    }

    /* Botão menu mobile */
    .menu-btn {
      background: #333; /* Cor mais escura para o botão */
      color: white;
      border: none;
      padding: 10px 15px;
      font-size: 1.2rem;
      border-radius: 5px;
      transition: 0.3s;
    }

    .menu-btn:hover {
      background: #444; /* Um pouco mais claro no hover */
    }

    /* Botão de fechar corrigido */
    .btn-close-white {
      filter: invert(1); /* Garantir que o ícone de fechar fique branco */
    }

    /* Cor do conteúdo principal */
    #mainContent {
      color: #333; /* Cor escura para o texto */
      background: #f4f4f4; /* Fundo claro para o conteúdo */
      padding: 20px;
      border-radius: 5px;
    }
    /* Estilo global */
    body {
      font-family: Arial, sans-serif;
      background: #f8f9fa;
      color: #333;
      padding-top: 70px;
    }

    /* Navbar fixa minimalista */
    .navbar {
      background: #333;
    }
    .navbar-brand {
      font-size: 1.5rem;
      font-weight: bold;
      color: white;
    }
    .nav-link {
      color: white;
      font-weight: bold;
    }
    .nav-link:hover {
      color: #dcdcdc;
    }

    /* Hero Section */
    .hero-section {
      background: #f5f5f5;
      text-align: center;
      padding: 100px 20px;
    }
    .hero-section h1 {
      color: #333;
      font-weight: bold;
    }
    .hero-section p {
      color: #555;
    }

    /* Seção de Benefícios */
    .features {
      background-color: white;
      padding: 50px 0;
    }
    .features h4 {
      color: #333;
    }
    .features p {
      color: #666;
    }

    /* Rodapé */
    .footer {
      background: #333;
      color: white;
      padding: 20px 0;
      text-align: center;
    }
  </style>
  </head>

  <body>

 <!-- Navbar fixa no topo -->
<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
  <div class="container">
    <!-- Logo com ícone e nome -->
    <a class="navbar-brand d-flex align-items-center" href="/">
      <i class="fa-solid fa-credit-card me-2"></i> <!-- Ícone de cartão de crédito -->
      Cobrar-me
    </a>

    <!-- Botão do menu no mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a href="<%= new_user_session_path %>" class="nav-link" id="btnEntrar">
            <i class="fa-solid fa-right-to-bracket me-2"></i> Entrar
          </a>
        </li>
        <li class="nav-item">
          <a href="<%= new_user_registration_path %>" class="nav-link" id="btnCadastro">
            <i class="fa-solid fa-user-plus me-2"></i> Cadastro
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>
    
  <!-- Conteúdo principal -->
  <div id="mainContent" class="container mt-4">
     <%= yield %>
  </div>


  <!-- Rodapé -->
  <footer class="footer">
    <div class="container">
      <p>&copy; 2024 Cobrar-me. Todos os direitos reservados.</p>
    </div>
  </footer>
  <!-- Carregamento correto do Bootstrap -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>


   
  </body>
</html>
